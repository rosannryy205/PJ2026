import React from "react";

/**
 * Placeholder Dashboard Admin.
 * Body sẽ được build sau — hiện tại chỉ là khung trống để
 * layout admin (header + footer + loading) render hoàn chỉnh.
 */
export default function AdminDashboard() {
  return (
    <div
      style={{
        minHeight: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        backgroundColor: "#ffffff",
        fontFamily: "SF Pro Text, system-ui, -apple-system, sans-serif",
        color: "#86868b",
        fontSize: "14px",
        letterSpacing: "-0.12px",
      }}
    >
      <span>Dashboard Admin — đang xây dựng</span>
    </div>
  );
}
